
// union type array 
const array = ['Ridoy', 'Roy', 'Rohid', 8457, true]

array.push("hey");

console.log(array)


// Tuple - we can use tuple while we want to define multiple types in an array
const user: [number, string] = [325, 'Name']

const girlfriend: [string, boolean] = ["Ridoy", true]